<resources>
    <!-- Application theme. -->
    <style name="AppTheme" parent="@style/BaseAppTheme">
        <!-- Status bar. -->
        <item name="android:statusBarColor">@color/black</item>
    </style>
</resources>